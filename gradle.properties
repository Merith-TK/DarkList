# Gradle Properties
org.gradle.jvmargs = -Xmx1G
org.gradle.parallel = true

# Mod Properties
version = 1.0.0
maven_group = tk.merith
archives_base_name = dark_list

# Dependencies are managed at gradle/libs.versions.toml
